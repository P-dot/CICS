# JCL members used

The lab used:
- `IBMUSER.CICS.JCL(L04MAP)` for BMS assembly/link/DSECT generation.
- `IBMUSER.CICS.JCL(L04COB)` for CICS translation, COBOL compilation and link-edit.

The screenshots are the authoritative evidence for the exact submitted members and return codes. The portfolio package records the member names rather than inventing JCL text not fully recoverable from the evidence.
