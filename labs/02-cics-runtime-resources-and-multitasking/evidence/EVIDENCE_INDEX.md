# Evidence index

The screenshots in `screenshots/` were extracted from the final execution document supplied for Lab 02.

They document the observed sequence:

1. CICS SYSTEM inquiry.
2. Initial `CH01` runtime inquiry returning `NOT FOUND`.
3. CEDA view showing the stored `CH01` definition in `LAB01`.
4. `CEDA INSTALL GROUP(LAB01)` with `INSTALL SUCCESSFUL`.
5. Successful runtime inquiry of `CH01`.
6. Successful runtime inquiry of `CICSL01`.
7. `CEMT I TASK` showing a live CEMT task.
8. Final successful execution of `CH01`.

Before public publication, screenshots should be visually reviewed for host-specific or sensitive information.
